<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextureView
        android:id="@+id/CameraIdFront_texture"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="80dp" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="80dp"
        android:src="@drawable/ic_idcard_skeleton_front" />

    <FrameLayout
        android:id="@+id/CameraIdFront_control"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:background="@color/control_background">

        <Button
            android:id="@+id/CameraIdFront_picture"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/btn_carmera_picture_selector"
            android:padding="5dp"
            android:rotation="90"
            android:text="@string/picture"
            android:textColor="@color/whiteColor"
            android:textSize="16sp" />

        <Button
            android:id="@+id/CameraIdFront_Cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="15dp"
            android:background="@color/transparentColor"
            android:padding="5dp"
            android:rotation="90"
            android:text="@string/cancel"
            android:textColor="@color/whiteColor"
            android:textSize="16sp" />
    </FrameLayout>

</RelativeLayout>
